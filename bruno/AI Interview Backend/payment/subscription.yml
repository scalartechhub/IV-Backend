info:
  name: Subscription
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/payment/subscription"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get authenticated user's current subscription details.
