info:
  name: Upload Job Description
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/interviews/{{interviewId}}/jd"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - C:\Users\ASUS\Downloads\job-description-React-JS-min-compressed.pdf
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: interviewId
      value: NrAXkcPxbvcY0fHR539a

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Upload job description PDF (multipart field: `file`).
  Update the file path or pick a PDF in Bruno before sending.
